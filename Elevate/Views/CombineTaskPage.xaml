<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Elevate.CombineTaskPage">

    <!--<ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20">

            <HorizontalStackLayout Spacing="10">
                <CollectionView x:Name="ProjectsCollectionView" Margin="0,20,0,0">

                </CollectionView>
                <Label Text="Here are your Projects listed" VerticalOptions="Center" />
                <Entry Placeholder="Search..." />
            </HorizontalStackLayout>


            <CollectionView x:Name="TodoItemsCollectionView"
                        Margin="0,20,0,0">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical"
                                 Span="4" 
                                 VerticalItemSpacing="10"
                                 HorizontalItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5">
                            <Border>
                                <VerticalStackLayout Spacing="5">
                                    <Label Text="{Binding Name}"
                                       FontAttributes="Bold"
                                       LineBreakMode="WordWrap"
                                       HorizontalTextAlignment="Center" />
                                </VerticalStackLayout>
                                <Border.GestureRecognizers>
                                    <DragGestureRecognizer DragStarting="OwnDragStarting"/>
                                    <DropGestureRecognizer DragOver="OwnDropOver" Drop="OwnDropGesture"/>
                                </Border.GestureRecognizers>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>-->

</ContentPage>